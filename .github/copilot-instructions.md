# Project instructions

- Use React and Ant Design for the frontend.
- Keep Vite asset URLs relative with `base: './'` so the app works in Google Apps Script HtmlService.
- Keep `public/Code.gs` and `public/appsscript.json` in the build output; CLASP uses `dist` as its root directory.
- Validate frontend changes with `npm run build`.